#JS_Elzero 

```js
window.alert("Hello From JS File");

document.write("<h1>Hello <span>Page</span></h1>");

console.log("Hello From JS File");
```

**Window** => is an object that has some built-in functions.

 In JavaScript, it is a common practice to end each statement with a semicolon **;**
( *If We don't write it The code will run but it isn't the best practice and omitting them can cause unexpected issues in some cases* ).

**alert** => most developers don't use it now .

**document** => is also an object that has some built-in functions.

**console.log()** => to print the message in the console only.